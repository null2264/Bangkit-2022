<resources xmlns:tools="http://schemas.android.com/tools">
    <color name="cardBackground">@color/white</color>
    <color name="buttonColor">@color/gray_100</color>
    <color name="colorRipple">#43F0F6FC</color>
    <color name="bottomNavBackground">@color/gray_900</color>
    <!-- Base application theme. -->
    <style name="Theme.GithubUser" parent="Theme.AppCompat.DayNight">
        <!-- ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="homeAsUpIndicator">@drawable/ic_arrow_left</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray_800</item>
        <item name="colorPrimaryVariant">@color/gray_900</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/gray_100</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="colorOnBackground">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
        <item name="android:colorBackground">?attr/colorSecondary</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ThemeOverlay.GithubUser.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Subtitle">
        <item name="android:textColor">@color/gray_300</item>
    </style>

    <style name="Theme.GithubUser.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_github_mark</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.GithubUser</item>
    </style>

    <style name="Theme.GithubUser.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@drawable/bg_search_view</item>
        <item name="queryBackground">@android:color/transparent</item>
        <item name="strokeColor">@color/purple_200</item>
        <item name="iconTint">?android:attr/textColor</item>
        <item name="iconTintMode">src_atop</item>
        <item name="android:searchIcon">@drawable/ic_search</item>
        <item name="searchIcon">@drawable/ic_search</item>
    </style>

    <style name="Theme.GithubUser.SearchView.AppBar" parent="Theme.GithubUser.SearchView">
        <item name="android:textColorHint">@color/gray_300</item>
    </style>
</resources>